# Docker Commands

Run container(s) in the background:
```
docker-compose up -d
```

List docker containers:
```
docker ps // old
or
docker container ls // latest
```

Pause running containers:
```
docker-compose pause
```

Unpause paused containers:
```
docker-compose unpause
```